#!/usr/bin/env node
/**
 * Shared configuration for Cesium Native build scripts.
 * Update CESIUM_NATIVE_REF here when bumping the pinned cesium-native version.
 */

/** Git tag of CesiumGS/cesium-native to clone and build. */
export const CESIUM_NATIVE_REF = 'v0.60.0'
