///
/// HybridCesiumViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridCesiumViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridCesiumViewSpecSwift.hpp"
#import "ReactNativeCesium-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::reactnativecesium;
using namespace margelo::nitro::reactnativecesium::views;

/**
 * Represents the React Native View holder for the Nitro "CesiumView" HybridView.
 */
@interface HybridCesiumViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridCesiumViewComponent {
  std::shared_ptr<HybridCesiumViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridCesiumViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridCesiumViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridCesiumViewSpec> hybridView = ReactNativeCesium::ReactNativeCesiumAutolinking::createCesiumView();
    _hybridView = std::dynamic_pointer_cast<HybridCesiumViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  ReactNativeCesium::HybridCesiumViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridCesiumViewProps const>(props);
  auto& newViewProps = const_cast<HybridCesiumViewProps&>(newViewPropsConst);
  ReactNativeCesium::HybridCesiumViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // ionAccessToken: string
  if (newViewProps.ionAccessToken.isDirty) {
    swiftPart.setIonAccessToken(newViewProps.ionAccessToken.value);
    newViewProps.ionAccessToken.isDirty = false;
  }
  // ionAssetId: number
  if (newViewProps.ionAssetId.isDirty) {
    swiftPart.setIonAssetId(newViewProps.ionAssetId.value);
    newViewProps.ionAssetId.isDirty = false;
  }
  // cameraLatitude: number
  if (newViewProps.cameraLatitude.isDirty) {
    swiftPart.setCameraLatitude(newViewProps.cameraLatitude.value);
    newViewProps.cameraLatitude.isDirty = false;
  }
  // cameraLongitude: number
  if (newViewProps.cameraLongitude.isDirty) {
    swiftPart.setCameraLongitude(newViewProps.cameraLongitude.value);
    newViewProps.cameraLongitude.isDirty = false;
  }
  // cameraAltitude: number
  if (newViewProps.cameraAltitude.isDirty) {
    swiftPart.setCameraAltitude(newViewProps.cameraAltitude.value);
    newViewProps.cameraAltitude.isDirty = false;
  }
  // cameraHeading: number
  if (newViewProps.cameraHeading.isDirty) {
    swiftPart.setCameraHeading(newViewProps.cameraHeading.value);
    newViewProps.cameraHeading.isDirty = false;
  }
  // cameraPitch: number
  if (newViewProps.cameraPitch.isDirty) {
    swiftPart.setCameraPitch(newViewProps.cameraPitch.value);
    newViewProps.cameraPitch.isDirty = false;
  }
  // cameraRoll: number
  if (newViewProps.cameraRoll.isDirty) {
    swiftPart.setCameraRoll(newViewProps.cameraRoll.value);
    newViewProps.cameraRoll.isDirty = false;
  }
  // cameraVerticalFovDeg: number
  if (newViewProps.cameraVerticalFovDeg.isDirty) {
    swiftPart.setCameraVerticalFovDeg(newViewProps.cameraVerticalFovDeg.value);
    newViewProps.cameraVerticalFovDeg.isDirty = false;
  }
  // debugOverlay: boolean
  if (newViewProps.debugOverlay.isDirty) {
    swiftPart.setDebugOverlay(newViewProps.debugOverlay.value);
    newViewProps.debugOverlay.isDirty = false;
  }
  // pauseRendering: boolean
  if (newViewProps.pauseRendering.isDirty) {
    swiftPart.setPauseRendering(newViewProps.pauseRendering.value);
    newViewProps.pauseRendering.isDirty = false;
  }
  // maximumScreenSpaceError: number
  if (newViewProps.maximumScreenSpaceError.isDirty) {
    swiftPart.setMaximumScreenSpaceError(newViewProps.maximumScreenSpaceError.value);
    newViewProps.maximumScreenSpaceError.isDirty = false;
  }
  // maximumSimultaneousTileLoads: number
  if (newViewProps.maximumSimultaneousTileLoads.isDirty) {
    swiftPart.setMaximumSimultaneousTileLoads(newViewProps.maximumSimultaneousTileLoads.value);
    newViewProps.maximumSimultaneousTileLoads.isDirty = false;
  }
  // loadingDescendantLimit: number
  if (newViewProps.loadingDescendantLimit.isDirty) {
    swiftPart.setLoadingDescendantLimit(newViewProps.loadingDescendantLimit.value);
    newViewProps.loadingDescendantLimit.isDirty = false;
  }
  // msaaSampleCount: number
  if (newViewProps.msaaSampleCount.isDirty) {
    swiftPart.setMsaaSampleCount(newViewProps.msaaSampleCount.value);
    newViewProps.msaaSampleCount.isDirty = false;
  }
  // showCredits: boolean
  if (newViewProps.showCredits.isDirty) {
    swiftPart.setShowCredits(newViewProps.showCredits.value);
    newViewProps.showCredits.isDirty = false;
  }
  // ionImageryAssetId: number
  if (newViewProps.ionImageryAssetId.isDirty) {
    swiftPart.setIonImageryAssetId(newViewProps.ionImageryAssetId.value);
    newViewProps.ionImageryAssetId.isDirty = false;
  }
  // onMetrics: optional
  if (newViewProps.onMetrics.isDirty) {
    swiftPart.setOnMetrics(newViewProps.onMetrics.value);
    newViewProps.onMetrics.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return ReactNativeCesium::ReactNativeCesiumAutolinking::isCesiumViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  ReactNativeCesium::HybridCesiumViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  ReactNativeCesium::HybridCesiumViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
